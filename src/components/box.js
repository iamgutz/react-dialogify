import styled from 'styled-components';

const Box = styled.div`
  display: inline-flex;
  background-color: #fff;
  color: #000;
  border-radius: 8px;
  z-index: 1;
  width: 100%;
  max-width: 40rem;
  margin: 1rem;
`;

export default Box;
